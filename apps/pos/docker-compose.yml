services:
  pos-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: pos-frontend
    ports:
      - "5205:5205"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true
