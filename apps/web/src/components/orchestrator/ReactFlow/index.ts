
import AgentNode from './AgentNode';
import WorkflowEdge from './WorkflowEdge';

export const nodeTypes = {
  agent: AgentNode,
};

export const edgeTypes = {
  workflow: WorkflowEdge,
};

export { AgentNode, WorkflowEdge };
